#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>

int main() {
  float val;
  printf("Enter a value for ceil, floor & round: ");
  scanf("%f", &val);
  printf("Ceil: %.0f\nFloor: %.0f\nRound: %.0f", ceil(val), floor(val), round(val));

  return 0;
}
// ceil
// floor
// round
// sqrt
// pow
// abs
#include <stdbool.h>
#include <stdio.h>
#include <string.h>

int main() {
  
  
  return 0;
}